<odoo>
    <data>
        <record id="contract_booking_expire_scheduler" model="ir.cron">
            <field name="name">Contract Booking Expire</field>
            <field name="active" eval="True"/>
            <field name="interval_number">12</field>
            <field name="interval_type">hours</field>
            <!-- <field name="numbercall">-1</field> -->
            <!-- <field name="doall" eval="False"/> -->
            <field name="model_id" ref="contract_and_offer.model_contract_offer"/>
            <field name="code">model._contract_booking_expiry()</field>
            <field name="state">code</field>
        </record>

    </data>
</odoo>